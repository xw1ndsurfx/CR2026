using Intersect.Localization;

namespace Intersect.Server.Localization;

public static partial class Strings
{
    public static class Poker
    {
        // Never call these Aureons while the registry uses temporary chips.
        public static LocalizedString NetWin = "[Poker] {00} wins {01} test chips (net gain).";
    }
}
